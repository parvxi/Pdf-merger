services:
  - type: web
    name: petrolube-pdf-merger
    env: docker  # ✅ Changed from 'python' to 'docker'
    dockerfilePath: ./Dockerfile
    plan: free  # or your plan type
    envVars:
      - key: PORT
        value: 10000
